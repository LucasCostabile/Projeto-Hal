<nav class="navbar navbar-expand-lg bg-body-tertiary" data-bs-theme="dark">
  <div class="container-fluid">
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/">Home</a>
        </li>

       {{#if user}}
        <li class="nav-item">
          <a class="nav-link" href="/logout">Logout</a>
        </li>
        {{else}}

        <li class="nav-item">
          <a class="nav-link" href="/login">Login</a>
        </li>
       {{/if}}
        <li class="nav-item">
          <a class="nav-link" href="/products">products</a>
        </li>
           
      </ul>

    </div>
              <div class="user-cart">
              <p class="user-name">Bem Vindo! {{userName}} </p>
              </div>
  </div>
</nav>
<h1> Carrinho</h1>
<table class="table table-striped">
  <thead>
    <tr>
      <th>ID Produto </th>
      <th>Item</th>
      <th>Preço</th>
      <th>Estoque</th>

    </tr>
  </thead>

  <tbody id="ler-produtos">
    {{#each productFoundCart.productsCart}}
      <tr>
        <td id="prodID">{{_id}}</td>
        <td>{{title}}</td>
        <td>R$ {{price}}</td>
        <td>{{stock}}</td>
        <td> <button id="btn-remove-cartprod">Remover</button></td>
      </tr>
    {{/each}}
  </tbody>
</table>
<script src="/js/cart.js"></script>